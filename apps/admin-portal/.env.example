# Neo-Lloyds Admin Portal — local development

NEO_LLOYDS_API_URL=http://localhost:3001

# --- WorkOS AuthKit (optional; see README.md) ---
# Unset -> the "Sign in with WorkOS" button on /login doesn't render, and
# the portal behaves exactly as before this was added (paste-a-credential
# login only).
#
# From the "Neo-Lloyds" WorkOS project, Staging environment
# (https://dashboard.workos.com -> Neo-Lloyds -> Staging -> API Keys):
WORKOS_CLIENT_ID=client_01M0DMPW9VVWVXC5PZHAEW6S2K
# WORKOS_API_KEY=sk_test_...   # from the same API Keys page -- never commit the real value
WORKOS_COOKIE_PASSWORD=          # 32+ chars, e.g. `openssl rand -base64 24`
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback

# Scopes sign-in to the Neo-Lloyds organisation created in that project
# (org_01M0DMQZE6GYX4PV7ZPG5JH6QN) rather than any WorkOS org in the account.
WORKOS_ORGANIZATION_ID=org_01M0DMQZE6GYX4PV7ZPG5JH6QN
